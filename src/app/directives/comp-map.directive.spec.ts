import { CompMapDirective } from './comp-map.directive';

describe('CompMapDirective', () => {
  it('should create an instance', () => {
    const directive = new CompMapDirective();
    expect(directive).toBeTruthy();
  });
});
